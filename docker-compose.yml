services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: timer
      POSTGRES_PASSWORD: timer_password
      POSTGRES_DB: timer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
